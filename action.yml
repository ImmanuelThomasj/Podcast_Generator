name: "Podcast Generator"
author: "Thomas"
DESCRIPTION: "Geneartes a feed for a podcast from a YAML file"
runs:
  using: "docker"
  image: "Dockerfile"
branding:
  icon: "git-branch"
  colour: "red"
inputs:
  email: 
    description: The commiter's email addredd
    required: true
    default: ${{ github.actor }}@localhost
  name:
    description: The Commiter's name
    required: true
    default: ${{ github.actor }}